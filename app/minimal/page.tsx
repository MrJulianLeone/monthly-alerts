export default function MinimalPage() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold">Minimal Page - No Auth</h1>
      <p>This page has no auth, no queries, just static HTML.</p>
      <p>If this loads, the layout is fine.</p>
    </div>
  )
}
